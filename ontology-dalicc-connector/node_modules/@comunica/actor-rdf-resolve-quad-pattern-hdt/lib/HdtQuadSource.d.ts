import type { IQuadSource } from '@comunica/bus-rdf-resolve-quad-pattern';
import type { AsyncIterator } from 'asynciterator';
import type * as HDT from 'hdt';
import type * as RDF from 'rdf-js';
export declare class HdtQuadSource implements IQuadSource {
    protected bufferSize: number;
    protected readonly hdtDocument: HDT.Document;
    constructor(hdtDocument: HDT.Document, bufferSize?: number);
    match(subject: RDF.Term, predicate: RDF.Term, object: RDF.Term, graph: RDF.Term): AsyncIterator<RDF.Quad>;
}
