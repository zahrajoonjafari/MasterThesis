"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HdtQuadSource = void 0;
const HdtIterator_1 = require("./HdtIterator");
class HdtQuadSource {
    constructor(hdtDocument, bufferSize) {
        this.bufferSize = 128;
        this.hdtDocument = hdtDocument;
        if (bufferSize) {
            this.bufferSize = bufferSize;
        }
    }
    match(subject, predicate, object, graph) {
        if (graph.termType !== 'DefaultGraph') {
            throw new Error('HdtQuadSource only supports triple pattern queries within the default graph.');
        }
        return new HdtIterator_1.HdtIterator(this.hdtDocument, subject, predicate, object, { autoStart: false, maxBufferSize: this.bufferSize });
    }
}
exports.HdtQuadSource = HdtQuadSource;
//# sourceMappingURL=HdtQuadSource.js.map