import * as E from '../expressions';
import type { Bindings, IExpressionEvaluator } from '../Types';
import type { AsyncEvaluatorContext } from './AsyncEvaluator';
import type { SyncEvaluatorContext } from './SyncEvaluator';
declare type Expression = E.Expression;
declare type Term = E.TermExpression;
export declare class AsyncRecursiveEvaluator implements IExpressionEvaluator<Expression, Promise<Term>> {
    private readonly context;
    private readonly subEvaluators;
    constructor(context: AsyncEvaluatorContext);
    evaluate(expr: Expression, mapping: Bindings): Promise<Term>;
    private evalOperator;
    private evalSpecialOperator;
    private _evalAsyncArgs;
    private evalNamed;
    private evalAsyncExtension;
    private evalExistence;
    private evalAggregate;
}
export declare class SyncRecursiveEvaluator implements IExpressionEvaluator<Expression, Term> {
    private readonly context;
    private readonly subEvaluators;
    constructor(context: SyncEvaluatorContext);
    evaluate(expr: Expression, mapping: Bindings): Term;
    private evalOperator;
    private evalSpecialOperator;
    private evalNamed;
    private evalSyncExtension;
    private evalExistence;
    private evalAggregate;
}
export {};
