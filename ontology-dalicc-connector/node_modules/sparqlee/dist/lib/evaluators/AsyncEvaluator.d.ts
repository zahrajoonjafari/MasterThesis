import type * as RDF from '@rdfjs/types';
import type { Algebra as Alg } from 'sparqlalgebrajs';
import type * as E from '../expressions/Expressions';
import type { Bindings } from '../Types';
declare type Term = E.TermExpression;
export declare type AsyncExtensionFunction = (args: RDF.Term[]) => Promise<RDF.Term>;
export declare type AsyncExtensionFunctionCreator = (functionNamedNode: RDF.NamedNode) => AsyncExtensionFunction | undefined;
export interface IAsyncEvaluatorConfig {
    now?: Date;
    baseIRI?: string;
    exists?: (expression: Alg.ExistenceExpression, mapping: Bindings) => Promise<boolean>;
    aggregate?: (expression: Alg.AggregateExpression) => Promise<RDF.Term>;
    bnode?: (input?: string) => Promise<RDF.BlankNode>;
    extensionFunctionCreator?: AsyncExtensionFunctionCreator;
}
export declare type AsyncEvaluatorContext = IAsyncEvaluatorConfig & {
    now: Date;
};
export declare class AsyncEvaluator {
    algExpr: Alg.Expression;
    config: IAsyncEvaluatorConfig;
    private readonly expr;
    private readonly evaluator;
    constructor(algExpr: Alg.Expression, config?: IAsyncEvaluatorConfig);
    evaluate(mapping: Bindings): Promise<RDF.Term>;
    evaluateAsEBV(mapping: Bindings): Promise<boolean>;
    evaluateAsInternal(mapping: Bindings): Promise<Term>;
}
export {};
